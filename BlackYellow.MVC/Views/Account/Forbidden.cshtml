@using System.Security.Claims;
<h1>
    Hello @User.Identities.First(u => u.IsAuthenticated &&
                                     u.HasClaim(c => c.Type == ClaimTypes.Role)).FindFirst(ClaimTypes.Role).Value
</h1>